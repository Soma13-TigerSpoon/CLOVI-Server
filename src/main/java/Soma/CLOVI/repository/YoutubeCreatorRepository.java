package Soma.CLOVI.repository;

import Soma.CLOVI.domain.user.YoutubeCreator;
import org.springframework.data.jpa.repository.JpaRepository;

public interface YoutubeCreatorRepository extends JpaRepository<YoutubeCreator, Long> {
}
